import random
dictionary = {}
for i in range(1,101):
    dictionary[i] = int(str(i)[0])
print(len(dictionary))
keys = list(dictionary.keys())
for key in keys:
    value = value(random.randint(100,300))
print(dictionary)
